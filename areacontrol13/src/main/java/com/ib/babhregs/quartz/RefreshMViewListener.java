package com.ib.babhregs.quartz;

import com.ib.system.quartz.BaseJobListener;

public class RefreshMViewListener extends BaseJobListener {


    public RefreshMViewListener(String listenerName) {
        super(listenerName);
    }
}
